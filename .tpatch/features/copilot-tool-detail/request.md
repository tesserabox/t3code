# Feature Request: Surface tool arguments in the Copilot work log. Extract arguments from the SDK tool.execution_start event and format a summary (e.g. Bash: git status) into the detail field of work log entries. Server-side only change in toolDetailFromEvent() — the UI already renders the detail field.

**Slug**: `copilot-tool-detail`
**Created**: 2026-05-13T07:15:33Z

## Description

Surface tool arguments in the Copilot work log. Extract arguments from the SDK tool.execution_start event and format a summary (e.g. Bash: git status) into the detail field of work log entries. Server-side only change in toolDetailFromEvent() — the UI already renders the detail field.
