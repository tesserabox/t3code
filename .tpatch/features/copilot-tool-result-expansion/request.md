# Feature Request: Store full tool results server-side and serve them on expand. Currently truncateDetail() caps output at 180 chars. Instead, store the full result and let the UI fetch it when the user expands a work log entry. Requires server (storage + API endpoint) and web (fetch-on-expand) changes.

**Slug**: `copilot-tool-result-expansion`
**Created**: 2026-05-13T07:15:47Z

## Description

Store full tool results server-side and serve them on expand. Currently truncateDetail() caps output at 180 chars. Instead, store the full result and let the UI fetch it when the user expands a work log entry. Requires server (storage + API endpoint) and web (fetch-on-expand) changes.
